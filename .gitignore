# User-specific config (use config.example.json as template)
config.json

# Auto-generated model cache
models.json

# Claude Code instructions (not needed for users)
.claude
CLAUDE.md
Akina.lnk

# Windows reserved name artifacts (created by bash errors)
nul
